paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development

    development:
        adapter: mysql
        host: localhost
        name: my_oauth2_db
        user: root
        pass: mani1234
        port: 3306
        charset: utf8
